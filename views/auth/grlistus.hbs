{{!-- isAdmin = false --}}

{{!-- 1 People --> "N" Groups
                    1  Group --> "N" Meetups
      1 People --> "N" Meetups  --}}

{{!-- We have all Groups for an _id.People in the token, we list first 
idGroups[].Group === _id.People and later other Groups --}}
{{!-- Table = group --}}

<h1>GROUPS</h1>


<h2>List of groups:</h2>
{{#if groupUpdated}}
    <p>{{groupUpdated}}</p>
{{else}}
    <p>You can see group excursions, join or leave groups</p>
{{/if}}

{{#each grListUsrAndUsr.data}}
    <div class="group_list_user">
        <p>{{this.groupName}}</p>
        <p>{{this.groupDescription}}</p>
        
        {{!-- <form action="/melistus/{{this._id}}" method="GET">
            <button type="submit">MEET UPs</button>
        </form> --}}
        {{!-- The ADD means (if enabled / REMOVE disabled) --> 
            a) In the group table to add _id.People to idPeople[].Group
            b) In the people table to add _id.Group to idGroup[].People 
            c) render this page (grlistus) with new situation
            
            The REMOVE means (if enabled / ADD disabled) -->
            a) In the group table to remove _id.People from idPeople[].Group
            b) In the people table to remove _id.Group from idGroup[].People --}}

        <form action="/melistus/{{this._id}}" method="GET">
                <button type="submit">MEET UPs</button>
        </form>
        <form action="/grlistus/remove/{{this._id}}" method="POST">
            <button type="submit">REMOVE -</button>
        </form>
        <form action="/grlistus/add/{{this._id}}" method="POST">
            <button type="submit">ADD +</button>
        </form>
    </div>
{{/each}}

<div>
    <form action="/grlistus/previous/" method="GET">
            <button type="submit">Previous</button>
        </form>
        <form action="/grlistus/next/" method="GET">
            <button type="submit">Next</button>
        </form>
</div>



