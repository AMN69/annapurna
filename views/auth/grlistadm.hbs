{{!-- isAdmin = true --}}

{{!-- 1 People --> "N" Groups
                    1  Group --> "N" Meetups
      1 People --> "N" Meetups  --}}

{{!-- We have all Groups for an _id.People in the token, we render Groups with 
idAdmin.Group === _id.People --}}
{{!-- Table = group --}}

<h1 class="h1-class">GROUP EXCURSIONS (admin)</h1>

{{#each groupListAdmin}}
<div class="container-list">
    <div class="list-row">
        <p class="p-title">{{this.groupName}}</p>
        <p class="p-descrip">{{this.groupDescription}}</p>
        <form action="/melistadm/{{this._id}}" method="GET">
            <button type="submit" class="button-editdetails">MEET UPs</button>
        </form>
        <form action="/grdetadm/{{this.id}}" method="GET">
            <button type="submit">EDIT</button>
        </form>   
    </div>

     <div class="buttons-addremovemeet">  
        {{/each}}

        <form class="form-create" action="/grcreadm" method="GET">
            <button class="button-create" type="submit">CREATE NEW GROUP</button>
        </form>     
    </div>
</div>


